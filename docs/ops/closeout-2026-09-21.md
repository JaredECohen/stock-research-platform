# MarketMosaic repair-batch closeout — September 21, 2026

The September 13 handover repairs, durable price/fundamentals storage and company backfill
are shipped. Both production services currently run `3376a936538800b89e2987f43a0dcab9d8850650`,
including the later HTMLParser and type-checking fixes. This closeout changes documentation
and local project memory only. Remaining work is recorded in `.claude/memory/FIXES.md` and
`.claude/memory/DEVPLAN.md`, with explicit owner gates.

## Current verification

The [compact evidence receipt](closeout-2026-09-21-evidence.json) records authenticated
read-only application and Render checks, exact intervals, raw-receipt hashes and metric samples.

- Both deployments are live: worker `dep-danjegvavr4c73agpa30` since September 20 at
  01:31:37 UTC; web `dep-danjegvavr4c73agp9k0` finished at 01:32:15 UTC. Health before/after
  and the worker heartbeat agree on the build.
- All 24 returned memo regeneration jobs and all 52 industry jobs succeeded; both queues
  have no queued/running jobs. These counts do not certify research quality.
- Exact ABBV v7 GET returns HTTP 200. Its entire response equals the retained September 13
  post-fix response, including text, metadata and original timestamps.
- A complete **retrospective 90-minute interval**, September 21 **02:55–04:25 UTC**, contains
  448 unique logs in five fully paginated responses and 91 consecutive minute metrics on
  worker instance `srv-d9valk942hec738ilv4g-24vpn`. No error/restart log signature or Render
  event occurred in that interval. Render memory peaked at **302.16 MiB**; process RSS
  samples peaked at **308.3 MiB**, with a lifetime high-water of **337.4 MiB**. These are
  different measurements. The 512 MiB worker's maximum concurrency headroom remains unproven.
- The actual 03:32 news completion note reports `5 material events; focus 21
  (pinned=9, regen_requested=2, memo_fresh=3, memo_stale=7); withheld 1 with no memo to
  patch (pinned_no_memo=1): BRK.B`. EDGAR completed at 03:02, 03:32 and 04:02 with
  `0 new filings; polled 170/170 in-tier, skipped 3 out-of-tier`. Scheduled history and
  scorecard work also completed; this interval included no memo or industry generation.
- Completion has coverage caveats: **163 warning records** include bounded source disclosures,
  rejected unprovenanced replacements, four FMP HTTP 402 responses, and price-coverage warnings.
  History names **208 bounded filing sources**. Scorecard run 13 scored **169/170**, with one
  insufficient company, three stale prices, two PIT exclusions and 256 missing period ends.
  The receipt retains all warning IDs, non-bounded warning messages, the complete history and
  postmortem notes, and the scorecard completion log. These limits remain distinct from a process
  failure or OOM; successful completion does not certify complete inputs.
- [CI on this exact revision](https://github.com/JaredECohen/stock-research-platform/actions/runs/35481577907)
  passed **3,680 tests / 13 skips**, with zero mypy errors across 19 files.
  [Nightly live integration](https://github.com/JaredECohen/stock-research-platform/actions/runs/35513484021)
  also passed. No new full application suite was needed for this documentation-only closeout.

## Data and historical preservation

At the current September 21 two-year floor, **170/173 companies** have complete current
price coverage. ANSS, AVB and EA retain their documented acquisition-related trading-end
exceptions. **172/173** have complete primary annual and quarterly fundamental periods;
ANSS is the exception. Returned targets total **186,965 price rows** and **277,469 fundamental
rows**. These are raw counts across 197 requested targets, including excluded observations,
not certified physical-table totals. No import was repeated during this closeout.

**Fundamental freshness is an open defect:** zero companies have all six statement/cadence
buckets within the seven-day fetch TTL, despite retained period coverage. The existing daily
history path does not establish fresh durable annual/quarterly coverage merely by succeeding.
FIX-005 records the evidence and requires a successful-provider-check contract that preserves
availability and never stamps cached data fresh. Legacy AUDC prices have also aged past current
coverage; the aggregate complete-target count is now 171 (170 companies plus SPY).

The current read-only audit returned **711 unique outcomes**, without truncation or exclusions.
All **710 previously captured rows / 9,940 stored field values remain identical**. The only new
row is **ADBE outcome 801**, written by the ordinary September 21 02:30 scheduler. No outcome
was recalculated, deleted or manually evaluated. Its completed note reports **59 unavailable
pairs** (57 without usable ticker prices and two with short histories); exact identities remain
in the evidence receipt. Fixture-like symbols are not authorization to delete historical rows.

The retained September 14 ac31 audit and census did finish: 710 unchanged outcomes, and
**173 companies / 21 with memos / 152 missing** at 00:24–00:27 UTC. Automatic TSLA v1 was the
only memo-existence change from the preceding census. That is a dated full census, not a new
September 21 census. Exactly one manual validation remains **META job 7 / v1**; this closeout
generated no memos. Its old $1.894251 estimate uses the pre-correction price table and is not
a billed amount or comparable to newly priced calls.

## Remaining work and decision boundaries

Project FIXES retains actionable entries for durable fundamentals freshness, bank/date
comparability, unpriceable legacy outcomes, industry forecast-validation rejection, narrative
arithmetic/units, representative worker capacity, safe test-guard failures, hermetic tokenizer
verification, and durable evidence backup. The original BK reconciliation is complete and must
not be repeated. Historical identities, availability dates and original outcomes stay preserved.

DEVPLAN now distinguishes shipped implementation from launch readiness. FEAT-001 charts are
implemented; FEAT-002 auth/billing is implemented but remains dark; FEAT-003 industry reports
run on the worker while analyst routing remains off. Notion's three feature rows say Done,
but their page bodies still describe planning. Two Active project rows also remain, one with
the stale AgenticAI repository path. Those external records were read, not modified.

Owner decisions remain separate: provider-key/Render-token rotation; research prompts and
publication policy; rating/confidence/KPI semantics and historical corrections; corpus reindexing,
learning enablement and broader memo seeding; Vertex/SDK/research-depth changes; paid plan
changes; auth/billing launch; taxonomy licensing and report access. The original handover
explicitly approved the news focus budget of 25. Its 600 possible focus slots/day are not proof
of 600 Gemini calls/day or a measured 2.5x spend increase.

The user's complete investment objectives/process are saved in
[the canonical research note](../research/Investment_Objectives_and_Process.md) and linked from
the repository's agent-facing documentation. No prompt or rating policy was changed to add them.

## Evidence limits

The original final ac31 observer result was not retained, and `/private/tmp/mm-handover` is gone.
That observation is **not still running and is not certified passed**. Today's complete later
interval establishes current behavior; it does not reconstruct the missing September 13 interval.
The earlier 65.55-minute `1f7f447` window remains **failed** for its ABBV news error.

The four curated historical review files remain tracked; roughly 41 MB of underlying receipts
remain local-only in `docs/reviews/`. Current raw GETs are local-only under
`.claude/memory/closeout-2026-09-21/`; their hashes and the compact current evidence are committed.
An approved backup location is still needed. No evidence was deleted or represented as backed up.
