# MarketMosaic handover closeout — September 13, 2026

Status: the original repairs, exactly one manual META validation, bounded-memory repairs and the durable market-data database are deployed. All company backfill attempts, BK reconciliation and targeted retries are complete; all 710 previously captured outcomes remain unchanged. Memo ownership, schema-startup and point-in-time eligibility guards are also deployed. The 65.55-minute `1f7f447` observation had no OOM or restart, but failed its functional news check because an older ABBV memo could not be read. The ABBV compatibility and industry-job ownership repairs are now deployed as `ac31dd8`; the exact ABBV v7 read passes. Observation of the replacement through the next full hourly interval is in progress; no passing final-runtime-window claim is made yet.

The research-quality assessment is a separate first-class deliverable: [production memo critique](2026-09-13-memo-research-critique.md). It evaluates AAPL v7, AMZN v2, AVGO v1, MSFT v108, GOOGL v6, and the one authorized META validation memo. Successful jobs and high reported hit rates are not evidence of completed or accurate research.

## Verified starting state

- Both Render services initially ran `33b615f`. The web service had OpenAI, Anthropic, FMP, Alpha Vantage, FRED and SEC identification configured. The worker had only FMP among those provider settings. Both services had live-data flags enabled.
- AAPL v7, AMZN v2 and AVGO v1 were saved with `generation_mode=demo`, real market inputs, and deterministic analysis. Empty degradation arrays did not establish live reasoning. GOOGL v7 subsequently completed automatically at 01:09:57 UTC; the critique retains the requested v6 captured before that replacement.
- Render records four worker OOM kills: September 12 at 21:29:14 and 22:06:20 UTC, then September 13 at 00:36:09 and 01:09:01 UTC. The handover's “no OOM since 22:06” statement was stale. GOOGL's successful job had two attempts and resumed after the last restart.
- The existing `?version=N` memo query was ignored. AAPL requested as v1 returned v7. The captured critique verified actual response headers; exact version retrieval is repaired in this release.
- The Docker image omits `config.env`. Neither service had model, Vertex, research-depth, or SDK overrides. Production therefore used class defaults. Copying the entire file would change execution routing and raise the ordinary follow-up question ceiling from two to nine; that was not done. [Configuration evidence and decisions](../ops/runtime-config-drift.md).

## Urgent release completed first

`7e7aabdf9ac9c30f40c1645f60c94a22a67f8157` merged and pushed the two urgent corrections, with the additional chunk-budget defects found during review. Worker deployment completed at **01:20:26 UTC**; web deployment completed at **01:20:54 UTC**. The public health endpoint reported the exact revision.

The chunker now measures dense Unicode and bounds numeric carry as well as sentence overlap. Independent adversarial numeric chunks measured 101, 400 and 474 tokens against the 500-token budget, with byte-identical no-overlap reconstruction. CJK, emoji, unbroken text, long sentences and table cases are covered. The intermediate progress correction shipped in this first release is superseded by durable, separate progress fields in the final release.

Both first-release full suites passed 3,057 tests, with one skipped test, eight intentional deselections, and only the documented blank-key configuration failure. The last numeric/whitespace refinement also passed 49 focused checks, lint, and Python 3.11 compilation. A subsequent read-only 17-sample Render snapshot through 01:35:26 UTC showed 137.9–222.0 MiB and no further worker failure; this short, mostly idle interval was not used to certify capacity.

## Final repair scope

- **Memory:** SEC downloads, decompression, decoding and HTML extraction stream before the existing 250,000-character text cap. Both retained and observed text sizes and every bounded accession are reported. Oversized tags, comments, scripts, CDATA, entities and compressed streams have independent adversarial coverage. The offline actual-fetch probe's 40 MiB document peaked near 542 MiB before the repair and 57 MiB afterward. This identifies a credible OOM mechanism; it does not prove which exact filing caused a production kill.
- **Ingestion:** filing risk/legal lists are normalized for indexing and diffing. Filing/transcript post-processing runs after the raw transaction commits, isolates failures per document, and reports each identity. A failed body fetch preserves an existing good body and prevents a successful event result. Marked incomplete rows can be completed on a later successful read. Previously failed historical chunks are not broadly rebuilt. Partial provider responses can remain in the existing seven-day body cache, so a visible failure can persist until refresh.
- **Monitoring:** completed timestamp, result and note remain intact while separate progress fields describe the current pass. Postgres is authoritative across processes. EDGAR retains retryable failed events and names provider, gate, persistence, post-pass, body-fetch, deferred and unvisited items. The loop inventory remains 21.
- **Outcomes:** price requests use age-based cache-friendly windows, reject remote baselines and invalid closes, and compare stock and benchmark on identical observed dates. Tiingo requests dated history. Missing benchmark coverage yields unavailable alpha. The new authenticated audit performs one read-only query over every persisted outcome, including orphaned rows. It does not certify unflagged legacy rows as clean. [Audit semantics](../ops/outcome-audit.md).
- **Postmortems:** existing work and policy deduplication no longer count as failures; policy is rechecked before spending. Deferred and failed snapshot IDs are explicit. Missing benchmark values display as unavailable. Memory writes obey the existing disabled flag and record actual success, partial failure and completion-flag failure. Old outcome rows are not rewritten.
- **Coverage:** provider share-class spelling resolves to canonical BRK.B. The approved news focus ceiling rises from **10 to 25 names per hourly pass**. This allows up to 600 scheduled focus opportunities/day if all slots run; it is not a service-wide spend cap, and explicit single-ticker calls use a separate path. Current production has no Gemini configuration and uses provider-news fallback. This is not a measured 2.5× increase in actual LLM spend.
- **Research consistency:** exact saved versions are readable without triggering regeneration; the original PM rationale is labeled when later risk/blend adjustments change its rating or confidence; duplicate confidence values agree. The critic sees the actual latest prior live memo, no longer runs an extra empty-draft paid call, and labels malformed/absent output as a rule-based fallback. Peer selection reports the actual basis; invalid EV/EBITDA comparisons name all exclusions; mentions per peer are not presented as a share of companies. Prompts, rating weights and headline KPI semantics are unchanged.
- **Runtime configuration:** production jobs refuse silent demo generation when no live reasoning provider is configured. Worker heartbeats show provider/mode, embedding configuration and build revision. The PM class default matches the repository's already selected Chat Completions model, without activating extra research rounds, the SDK or Vertex.

## Final validation and production evidence

The integrated runtime revision `09d0d21` passed **3,270 backend tests in each of the development and pinned-CI environments**, with one skipped test, eight intentional deselections and only the documented blank-key configuration artifact. Both suites used independent fresh SQLite databases, disabled live data, blank provider keys and the network guard. Lint and Python 3.11 compilation passed. Frontend validation passed **643 tests across 62 files** and a production build. All seven in-process application smoke prompts succeeded with demo data and blank provider keys. Test failures found during combined validation were repaired rather than waived: nullable benchmark postmortems, the required route inventory entry, and the intentional additive critic schema snapshot.

The code merged cleanly as `a51e674a069ebe0f6bb85adf33bcf3d6cb4209e7`, with a tree identical to the tested branch. Worker deployment `dep-daivvnajnfac73ekcf7g` became live at **01:44:26 UTC**; web deployment `dep-daivvnajnfac73ekceo0` became live at **01:45:07 UTC**. The health endpoint confirmed that revision. The first worker heartbeat on it reported `rss=151MB; llm=anthropic; generation_mode=live; embeddings=openai; build=a51e674a069ebe0f6bb85adf33bcf3d6cb4209e7`.

Before this deployment, five existing web settings were copied to the worker and verified equal: OpenAI, Anthropic, Alpha Vantage, FRED and SEC user identification. Values remained private and no credential was rotated. Final review then identified the analogous filing-memory write-reporting defect. The follow-up `740096f4279a294bc2969f12a6f60f6ba14bb4c3` passed **3,278 tests in each full backend suite**, with the same sole permitted configuration artifact, one skipped test and eight deselections. Lint and Python 3.11 compilation passed. Worker deployment `dep-daj03cfqj5pc73ar2tog` completed at **01:52:18 UTC**; web deployment `dep-daj03cfqj5pc73ar2t9g` completed at **01:52:29 UTC**. Both the public health build and worker heartbeat confirmed the exact revision before META submission. The META run described below followed this deployment.

### Exactly one manual validation memo

The single POST was accepted at **01:53:42 UTC** as **job 7**, run ID `fc14b8b7-7c29-4be2-922b-22c1b28d3aab`, with no prior META memo. It completed at **02:01:11 UTC** on **attempt 1**, in **447.4 seconds**, and saved **META v1**. Exact-version response headers were checked before capture. The local submission ledger prevents a blind retry. [Submission, job, call trace and selected log evidence](2026-09-13-META-validation.json); [readable saved memo](2026-09-13-META-v1.md); [complete saved JSON](2026-09-13-META-v1.json).

META is `generation_mode=live`, **Neutral / 70.8 confidence**. The trace has **37 logged calls**, including live Anthropic specialists and OpenAI PM synthesis. Its old counter records one failed call, but worker logs show **four Anthropic-to-OpenAI JSON-related failovers**. The risk committee ultimately failed to produce usable JSON and correctly appears as `review_mode=rule_based`, with `CriticUnavailable` and `ProviderFailover` degradation records. This validates honest failure reporting, not a completed live adversarial review. The application estimates **$1.894251**, not a billed total: the failed OpenAI JSON response lost its usage in the old accounting path, and embedding costs are not established by this trace.

During the META run, Render minute samples peaked at **227.1 MiB**, while process `ru_maxrss` telemetry reached **277.1 MiB**. The application log labels these numbers “MB,” but `memory_probe.py` divides by powers of 1024; this report normalizes them as MiB. The higher process high-water mark is retained in the sizing assessment. Neither measurement alone covers subsequent concurrent ingestion.

The run did execute filing post-passes for META filing IDs **2023, 2024 and 2025**, accessions **0001628280-26-050705, 0001628280-26-050596 and 0001628280-26-039193**. Logs show successful embedding requests and pgvector population of 10 and 23 rows during that activity, plus later successful query-embedding requests. Memory writes correctly report `disabled`. This is evidence of fresh ingestion activity; without a source-row census or saved retrieval payload it does not establish a complete corpus migration or a controlled chunker A/B.

### Repairs discovered by the validation

Two narrow corrections follow the preserved META output. `8771d29` parses JSON before recording provider success and resetting breakers, retains received usage on parse failure, and records safe termination metadata. `c34d96d` preserves each retrieved filing's own accession/document identity in saved citations, source lists and provenance metadata; unknown identities are explicit. Its regression test verifies identical outgoing filing-analyst requests when only citation metadata changes. Neither correction rewrites META v1 or historical metrics. Prompts, model budgets, ratings, DCF limits and publication rules remain unchanged. [JSON accounting](../ops/llm-json-accounting.md); [citation provenance](../ops/filing-citation-provenance.md).

The combined code revision **`21dba34559aa9f7422c569e7c3a2c23de8939186`** passed **3,327 tests in both full backend environments**, again with only the permitted configuration artifact, one skipped test and eight deselections. Lint and Python 3.11 compilation passed. It was pushed after those checks. Worker deployment `dep-daj0do15efls739beeg0` became live at **02:14:24 UTC**; web deployment `dep-daj0do15efls739bee10` became live at **02:14:44 UTC**. Health and the worker heartbeat confirmed the exact revision. The subsequent observation covered **02:14:24.196014–03:17:45 UTC**, but **failed**: Render recorded `oomKilled` at **03:00:03.069388 UTC**, event `evt-daj13crncjis73a7re9g`, against the 512 MiB limit. EDGAR had not completed its pass; postmortems had begun approximately 2.56 seconds before the kill. Render minute samples peaked at **455.949 MiB** and application process high-water telemetry reached **524.6 MiB**. The worker rebooted with the same Render instance identifier, so that identifier does not prove uninterrupted process lifetime. An approval timeout interrupted active polling between approximately 02:30 and 03:17; the interval was recovered from Render metrics and complete application logs, not continuously sampled by the observer. A separate later failure on the same old `21dba34` build was reported by Render at **03:32:39.221679 UTC**. Application logs bound that restart between the last old-process line at 03:31:26.555745 and boot at 03:31:29.551514; Render's event time is a report timestamp, not the exact death time. Historical filing/vector work overlapped the 03:30 EDGAR pass, but the allocation responsible for this second kill is unproven. Paced time-segmented log recapture verified the earlier 805-line window and recovered 17 lines skipped by cursor paging in the later window. No second manual memo was submitted. [Failure and memory-probe evidence](2026-09-13-runtime-failure-evidence.json).

### Additional memory repairs and durable data release

The failed observation exposed two more unbounded metadata scans and an analogous outcome scan. Postmortem selection previously hydrated all memo JSON before applying the 25-item pass budget; it now streams projected metadata and loads only a selected memo body. A probe using 360 memos with 512 KiB bodies reduced additional RSS from **368.9 to 6.2 MiB**, preserving 25 selected and 335 explicitly deferred identities. Cache-lineage traversal previously loaded 500,000 metadata rows, including empty lineage; streaming retained the same three reachable descendants while reducing additional RSS from **183.4 to 3.5 MiB**. The outcome sweep now reads bounded metadata pages and hydrates one body only after existing/not-due guards; a 360-already-recorded-memo probe reduced additional RSS from **371.0 to 4.8 MiB**, with zero writes and identical existing counts. These are controlled offline allocation measurements, not production capacity guarantees. The postmortem allocation is a strong explanation for the kill shortly after that loop started, rather than a captured allocation trace of the killed process.

The user then explicitly requested API-backed prices and fundamentals for every company, with at least two years of history and older dates where needed. Release **`ccf706cfdf9178eebf988149253ca5eaea46e062`** creates durable `daily_prices` and import-status tables alongside the existing `financial_periods`. Price identities include source and close basis; reads select one compatible series. The plan includes every company tier, SPY, and legacy memo symbols, using the earlier of two calendar years ago or the oldest live memo minus seven days. Annual and quarterly fundamentals retain fiscal periods, currency, availability and named-provider provenance; malformed periods, conflicting facts, incomplete history and freshness failures remain explicit. Existing financial rows are latest reported/restated values, not an archive of every historical revision. No new scheduler, memo generation or outcome evaluation is part of the import. [Storage and API contract](../ops/durable-market-data.md).

That combined tree passed **3,460 tests in each full backend environment**, with one skipped test, eight intentional deselections and only the permitted blank-key configuration failure. Both used fresh databases and the network guard. Lint, Python 3.11 compilation, independent backfill review and the 28 focused durable-price regressions passed. Frontend code is unchanged from its previously verified 643-test/build result. The merged tree is byte-identical to the tested branch. [Final validation record](2026-09-13-final-data-validation.json). Worker deployment `dep-daj1rp0u01pc738og8b0` became live at **03:52:30 UTC** and web deployment `dep-daj1rp0u01pc738og7s0` at **03:52:55 UTC**. Health and the new worker heartbeat confirmed the exact revision. The initial import began at **03:53:19 UTC** with SPY and targets 173 companies plus 23 legacy memo symbols. Its requested floor is **September 13, 2024**; available price responses commonly extend to July 3, 2023. This is an intermediate import, not the final corrected coverage certification.

### Corrections from the first database import

The initial 197-target pass finished at **04:19:42 UTC**. It reported 116,087 committed financial writes and 25,779 source upgrades; these are write counts, not distinct database row totals. A subsequent stored-only global read found two-year price coverage for **169/173 companies**, and complete primary annual/quarterly buckets for **163/173**. The original stricter success check passed 98 companies because it also treated optional legacy NULL fields as blockers. The corrected pass and final global read supersede those intermediate coverage numbers.

The follow-up repairs reported-zero debt/dividend values, prefers the provider's reported total debt, and corrects a legacy currency-default defect only when a fresh named provider confirms the corresponding statement. It corrects fiscal-year labels through exact provider-confirmed statement/date/line identities, checks the entire destination chain for collisions, and preserves row IDs and availability in one transaction. Ordinary FMP statement mapping also honors explicit fiscal years. For MSFT and NVDA, 128 undated bare-year legacy rows each remain unchanged and visible as unusable observations; they no longer block separately verified dated canonical facts. Their warnings become nonblocking only when a valid named-provider replacement exists. Every source/value/currency change, relabel, and retained unusable identity is captured in the [data audit](2026-09-13-market-data-backfill.md).

BK uses BNY's verified same-security ticker history while retaining its canonical company and memo keys. ANSS, AVB and EA have acquisition-related trading end dates; successor companies with different share terms are not aliases. Explicit zero-volume flat-OHLC vendor placeholders remain stored as raw observations but do not count as trades, current prices or usable coverage. Selection requires continuity against observed usable SPY sessions within the requested range, selects one provider/basis, and ties fetch freshness to the newest usable observation. Successful financial imports clear that ticker's shared statement cache; a failed invalidation leaves the import incomplete and is retried even after a no-op database refresh. [Storage, selection and refresh contract](../ops/durable-market-data.md).

The corrected code merged as **`a330420c0472487203d1b9ee28b371c92a9534a2`**, with a tree identical to tested branch `21fa4b7`. Both full backend environments passed **3,512 tests**, with one skipped test, eight intentional deselections and only the permitted blank-key configuration failure. Fresh databases, disabled live providers and the network guard were used. Ruff, Python 3.11 compilation, 277 focused fundamental tests, 91 focused price/outcome tests and independent review passed. Frontend code remains unchanged from its verified 643-test/build result. [Corrected-release validation](2026-09-13-corrected-data-validation.json).

### Final reconciliation release

The corrected 174-target pass completed at **04:54:07 UTC** on `a330420`: every one of the 173 companies and SPY received a fresh request. It exposed an important capability distinction: the verified BK→BNY price rename did not validate BNY historical fundamentals. The saved BNY statement response conflicts with canonical BK on fiscal labels. Final code requests canonical BK fundamentals and supplies an exact-fetch-time, digest-fenced repair plan with full before/after identities. This is a correction to the importer introduced during this work, not a preexisting provider issue alone. Reviewed plan `631c715e-ddc9-49d1-8311-9bf3eea7ef43` was applied atomically: **2,041 rows restored and 518 duplicate-destination rows quarantined**, with 416 unaffected rows fenced against concurrent changes. All affected IDs, period-end dates and availability fields were preserved, with full before/after evidence and no company, memo or outcome changes. Independent readback exactly matched the reviewed plan. Quarantine namespace `~Q686e51a5eebc6a` is excluded from normal company reads. [Repair contract](../ops/bk-fundamental-reconciliation.md); [complete data evidence](2026-09-13-market-data-backfill.md).

The final database-only coverage capture at **06:37:39 UTC**, with exact `1f7f447` health builds before and after, confirms **170/173 companies have current two-year price coverage** and **172/173 have complete, fresh primary annual and quarterly fundamentals**. Price exceptions are ANSS, AVB and EA, all with acquisition-related trading ends; ANSS also has incomplete current fundamentals. All 173 companies were attempted, and the Company universe still contains 173 unique tickers. Across the 197 returned company, benchmark and legacy targets, storage contains **177,092 price rows and 277,469 fundamental rows**, plus the separately audited 518 quarantine rows. These returned-target sums include preexisting and excluded observations; they are not certified physical-table totals. The price total rose by 1,288 between the preserved 05:16 capture and this read; that is a net storage change, not an additional import-write count or proof of which background job caused it. SPY and AUDC bring complete price targets to 172/197. Every incomplete legacy symbol and missing session remains in the data audit. Final targeted BK, LULU, ARM, PLTR and SNOW retries succeeded by **05:15:18 UTC**.

Fiscal reconciliation does not certify economic comparability. For BK, canonical FMP Q1 revenue $9.863bn less $4.454bn interest expense reconciles to issuer net revenue $5.409bn, whereas Q2 already reports net revenue $5.698bn. This is a mixed-definition series, not evidence by itself of a different issuer. Q2 provider EPS and diluted shares also differ from the issuer. No bank-specific normalization was guessed. [Provider and issuer comparison](2026-09-13-bk-provider-comparability.json).

The same release preserves LULU's two populated legacy debt duplicates while excluding them only when a unique valid provider observation confirms the same economic period. For ARM, PLTR and SNOW, older legacy-label lookbacks no longer force a second provider when the first already covers the required two-year history. MDT's 32 FY2018 period-end conflicts remain identified and unchanged; its current primary history is complete, and those older dates will not be guessed to make an import report pass.

The final merge **`82b2a2f52c441b8084a4e778d54321973adca9f8`** has the exact tree tested at `d533d39`. Both full backend environments passed **3,548 tests**, with one skipped test, eight intentional deselections and only the permitted blank-key configuration artifact. The two initially failing frozen schema checks were updated to explicitly include the new financial-repair audit model and table, then both full suites were rerun against new databases. Ruff and Python 3.11 compilation passed. [Final repair validation](2026-09-13-final-repair-validation.json).

A separate price-only request extended SPY for the 1,900-bar window observed in automatic LLY job 8. At **05:04:48 UTC**, its stored read returned 1,900 dates from **February 21, 2019 through September 11, 2026**. Subsequent DB-only LLY and SPY reads match on every one of those dates, with verified within-window benchmark continuity and no missing observed sessions. This extends the benchmark beyond the two-year minimum without another memo or outcome calculation. [Full benchmark evidence](2026-09-13-benchmark-history-extension.json).

The user's three investment styles and research sequence are saved in durable Codex memory and the local shared [investment objectives and process reference](../research/Investment_Objectives_and_Process.md). The new reference is explicitly a local addendum; a matching ChatGPT project-source upload has not been verified. This persistence request does not change the deployed stock-agent prompts, rating formulas or DCF assumptions.

### Final runtime guards from adversarial review

The existing filing scheduler generated automatic LLY job 8 and META job 9, separately from the sole manual META job 7. META job 9 exposed a rolling-deployment race: old worker `gk46d` began it at **05:06:40 UTC**, replacement `pkbcm` recovered it at **05:07:27**, and the old worker still completed a provider request at **05:07:48** before stopping at **05:08:22**. The replacement saved META v2 at **05:13:58**. Overlapping execution is established; duplicate final publication is not. The saved manual META v1 remains the critique and validation artifact.

The repair gives each attempt an immutable ownership token and a renewable 120-second lease. The existing polling loop recovers only expired owned work. Paid dispatch checks ownership, and checkpoint writes, progress, publication and completion enforce it transactionally. Publication records the exact memo version on the job in the same transaction, so a crash after saving a memo can complete recovery without rerunning the graph. Stale attempts cannot publish or alter a replacement's completion or charge. An already in-flight provider request cannot be undone; this is not a claim of exactly-once external billing under every failure. Legacy unowned jobs are explicitly reported and deferred. The first rollout must drain old jobs and verify the predecessor's shutdown and absence of stranded legacy work. [Ownership and rollout contract](../ops/regen-ownership-leases.md).

Review also found the worker could start its queue before the asynchronous seed reconciled new database columns. A synchronous database-only bootstrap now precedes worker threads and web readiness. It verifies the actual mapped job query, so swallowed migration errors cannot allow a process to start with missing ownership fields.

The final LULU capture revealed rows **35607 and 35609** whose recorded availability, **April 18, 2025**, predates their period end, **February 1, 2026**. Both the public point-in-time reader and the actual scorecard execution path previously admitted those future facts. A shared read-side predicate now requires known, completed periods and credible availability; invalid and unknown rows retain complete source, ID, date and reason diagnostics. The execution path persists that audit in its existing context field. No stored dates, historical scores, memos or outcomes are rewritten, and no live score rerun is requested. This guard closes the demonstrated temporal leak without claiming a revision archive. [Point-in-time eligibility contract](../ops/scorecard-pit-eligibility.md).

The guards merged as **`1f7f44751721a871579614160bb53f52bf1728db`**, with a tree identical to tested `a914017`. Both full backend environments passed **3,583 tests**, with one skipped test, eight intentional deselections and only the documented blank-key configuration artifact. Each used a fresh database, disabled live data and the network guard. Ruff, Python 3.11 compilation and diff checks passed. Independent focused review covered ownership, startup ordering and the actual PIT scoring path. [Full final runtime validation](2026-09-13-final-runtime-validation.json). The merge was pushed after a production snapshot showed zero queued/running memo jobs; the old EDGAR pass had just begun, so this snapshot alone did not prove an atomic drain.

Worker deployment `dep-daj3dh0ae00c73droi20` became live at **05:38:44 UTC**; web deployment `dep-daj3dh0ae00c73drohj0` at **05:39:32 UTC**. Health and the worker heartbeat confirm the exact revision. New instance `bh6pl` logged boot at 05:38:44.862808, repaired the ownership schema before queue startup at 05:39:03.958, and became ready at 05:39:04.061. Old instance `pkbcm` received SIGTERM at 05:39:43.741899 and its main thread logged “worker stopped” at 05:39:43.771111, but the old EDGAR executor continued SEC reads through 05:40:13.655 in the recovered log tail. That main-thread log is not an exact process-death timestamp. Post-replacement reads at **05:40:16 and 05:44:17 UTC**, after the old executor tail, showed all nine memo jobs succeeded and zero queued/running memo jobs, hence no stranded legacy-unowned running memo rows. No new manual memo was requested. The completed observation runs from **05:38:44.046214 through 06:44:17.139613 UTC**.

### Completed `1f7f447` window: stable process, failed news update

The **65.5516-minute** window has no recorded OOM, worker restart or lease failure. The maximum observed process-current RSS is **413.8 MiB**, separately reported high-water **412.9 MiB**, and Render minute-sampled maximum **393.410 MiB**, against the 512 MiB plan. The process maximum occurred at **06:30:15.060 UTC** while fetching OXY accession `0001628280-26-053377`, before NVDA job 14 started at 06:30:15.978; it must not be attributed solely to NVDA. Minute samples and process probes can miss instantaneous peaks. [Complete failed-window evidence](2026-09-13-runtime-1f7f447-evidence.json); [RSS chart with failed-acceptance label](2026-09-13-worker-1f7f447-rss.png).

The same worker completed EDGAR at **06:21:55 UTC**, recording **15 filing events**, all 170 in-tier companies visited, three out-of-tier exclusions, **49 named deferred companies** and **22 named bounded accessions**. These are events, not a claim of 15 unique documents. Every deferred ticker, accession and retained/observed size is preserved in the raw completion note and evidence. A second EDGAR pass started at 06:39:03.

The same-instance hourly news note at **06:41:14.743 UTC** is: `5 material events; 1 updates failed: ABBV; focus 20 (pinned=8, memo_stale=12); withheld 2 with no memo to patch (pinned_no_memo=2): BRK.B, TSLA`. Its result is **false**. The worker logged an ABBV `ValidationError` at 06:40:05; a read-only exact-version GET independently returned HTTP 500 because the stored v7 `bull_case` is a list while the current schema requires a case object. Thus the functional observation failed, despite stable process memory. This is not described as a provider outage.

All **100 warning/error records** were reviewed and retained. They include 42 transcript provider misses with stale cached rows, 28 model failovers, the ABBV validation error, and **eight rejected industry-report attempts** for groups **1010, 1510, 2010, 2020, 2030, 2510, 2520 and 2530**. The industry validator rejected unsupported claims; those records are not evidence that all eight jobs had exhausted retries. At 06:41:41 the industry heartbeat reported seven deferred jobs, zero failed-today jobs and 26 queued jobs; industry job 9 subsequently started for group 2550. An empty memo regeneration queue therefore did not mean all industry work was idle. Research guard rejections remain distinct from process failures and from the saved-memo compatibility bug.

### Repairs following the failed news observation

The ABBV repair projects a known legacy case-list shape into the current read schema without rewriting the saved row. It preserves original text, order and metadata in explicit degradation provenance. Mixed or malformed shapes still fail. New news patches and memo publications validate the current schema before persistence, closing a separately reproduced malformed-write path; that reproduction does not establish how historical ABBV v7 was created. At **23:21:56 UTC**, the exact saved v7 GET on `ac31dd8` returned HTTP 200 with the original generated timestamp and version/cache headers. Its projected case text exactly matched the retained original list, its metadata identified the same snapshot, and the complete response passed the current schema. Matching health builds bracketed the read. No memo was generated and no row was rewritten. [Before/after compatibility evidence](2026-09-13-ABBV-v7-compatibility.json). [Legacy compatibility contract](../ops/memo-legacy-case-compatibility.md).

A separate review found the industry queue repeated the old memo rolling-deployment race: boot recovery could requeue a live predecessor's job, and its stale execution could still publish. Industry attempts now have immutable ownership with renewable leases, fenced paid dispatch and database writes, and report/snapshot publication receipts committed with their output. Expired-only recovery preserves existing retry limits, backoff, final deterministic fallback and schedules. A bounded admin operation can reconcile explicitly identified legacy jobs only after the operator establishes predecessor retirement; ambiguous publications are rejected with all candidate IDs. [Industry ownership and cutover contract](../ops/industry-job-ownership.md).

Both initial combined suites on `a117ce8` reported 3,661 passes and five industry failure-path failures, in addition to the permitted blank-key configuration artifact. The failure-path fixtures froze the queue clock but left the new ownership clock at wall time, making test claims immediately expire. These failures are retained as a failed validation run. The corrected fixtures share one queue, report-store and lease clock and explicitly check that a later failed attempt postdates the prior report. Every original behavior assertion remains intact; no production ownership check is relaxed.

The final combined revision **`ac31dd875901a926704de3f579289234e871b606`** passed **3,666 tests in both full backend environments**, with one skipped test, eight intentional deselections and only the documented blank-key configuration artifact. Independent route/authentication tests passed 171 checks. Ruff and Python 3.11 compilation passed. The tested tree `574e85c6bb4e9c9c306dd994e3b00b358583b048` was fast-forwarded to main without changing its contents and pushed at **23:18 UTC**. [Failed and successful full-suite receipts](2026-09-13-industry-legacy-validation.json). Worker deployment `dep-dajiubdg1s2s73chdq30` became live at **23:18:34 UTC**; web deployment `dep-dajiubdg1s2s73chdpk0` became live at **23:21:15 UTC**. Public health confirms the exact revision. Worker `bw7hg` repaired the three industry ownership columns before starting either queue, then became ready at 23:18:48. The predecessor main thread stopped at 23:19:33; startup and transition evidence retain its separate tail. The replacement runtime observation remains in progress.

### Complete deferred identities in daily-loop notes

During the unchanged `1f7f447` observation, the normal daily transcript pass completed at **06:02:53 UTC** with 32 new transcripts, 170 in-tier companies polled and three out-of-tier exclusions. Its note reported 87 deferred companies but retained only **CMCSA, CMG, COF, COP and COST**, followed by “+82 more.” The remaining 82 identities were not logged or persisted by that pass and are not reconstructed or invented here. Poll completion also does not establish 170 usable transcript responses: the captured warnings identify 42 provider misses whose cached rows exceeded the 30-day fallback ceiling, plus a separate transcript timeout. Full captured warning identities and reasons are retained in the runtime evidence; these misses alone do not prove an entitlement denial.

The shared `note_names` helper also served history-backfill deferrals. The narrow follow-up removes abbreviation and retains every supplied identity in both completion notes and logs, while preserving work budgets, schedules, seen-state and processing decisions. Actual SQLite/status/log regressions exercise 87 deferred identities and prove that only the budgeted tickers are processed. Both full suites passed **3,585 tests**, with the same sole permitted configuration artifact, one skipped test and eight deselections; Ruff and Python 3.11 compilation passed. Local merge **`b36b3bf25137497f478907695a0d4f3104f1ed6f`** is identical to tested `d735c59`. [Formatter follow-up validation](2026-09-13-deferred-note-validation.json).

This output-formatting follow-up is deployed in `ac31dd8` alongside the ABBV and industry-ownership repairs. Its revised daily transcript/history notes are verified offline; the normal next scheduled passes are **September 14 at 06:00 and 03:15 UTC**, respectively. No extra pass, provider call or memo is triggered to make the report appear complete. The capacity observation remains attributed to the exact revision that performed it.

### Automatic regeneration during the completed `1f7f447` observation

The existing schedulers, without another manual request, produced five owned jobs on `1f7f447`. Transcript events generated **AAPL job 10 / v8** (completed 06:08:45 UTC), **AMZN job 11 / v3** (06:15:48) and **AVGO job 12 / v2** (06:23:32). Filing events generated **MSFT job 13 / v109** (06:30:15) and **NVDA job 14 / v231** (06:35:54). All five succeeded on attempt 1; periodic reads observed renewable ownership, and terminal receipts retain the exact published versions. The **06:36:17 UTC** snapshot showed all 14 historical/current jobs succeeded and none queued or running. These are completion and ownership observations, not new research-quality certifications; the critique still refers to the six saved versions identified above.

### Historical audit and coverage baseline

The read-only audit returned **all 709 historical outcomes**, independently recomputed with no classification discrepancies. **336 are late-evaluation candidates, all at 30 days**; the other **373 are unverified**, since all legacy rows lack baseline-date metadata. **615 of 709** outcomes belong to NVDA or MSFT, and 15 use TST-prefixed symbols absent from the company table. These observations further limit the headline's interpretation. [Full audit, all candidate identities and raw baseline](2026-09-13-outcome-audit.md).

Production also returned GOOGL **v6** with the original generated timestamp, rating and PM text while its latest history entry was v7, independently verifying the saved-version repair.

The pre-META census verified **173 companies, 18 with memos and 155 without**; all missing names are preserved in the audit. BRK.B is present as Berkshire Hathaway Inc. in the `auto_analysis` tier, without a memo. The intermediate census completed at **03:23:01 UTC**: **173 companies, 19 with memos and 154 without**, with every missing name preserved in the audit. Only META gained a memo between those censuses. The read-only follow-up at **03:19:28 UTC** compared all 14 stored fields for every original outcome: **all 709 were unchanged**, with no missing IDs. The normal scheduled outcome pass added only **NVDA outcome 800**, bringing the count to 710. Its note records actual stock and benchmark dates. The pass reported `evaluated=1560 due=768 written=1 existing=709 unavailable=58 no_prices=58 short_history=0 window_gap=0 unevaluable=0 reflections=0 errors=0`, with `success=false`; the 58 missing pairs prompted the user-authorized historical-data work. [Complete comparison](2026-09-13-outcome-audit-comparison.json).

After the completed imports, a new read-only comparison verified **all 710 outcomes unchanged across all 14 stored fields**, with no missing or new IDs relative to the 710-row follow-up. Stored-price readiness still reports 58 unavailable pairs: 56 lack a usable ticker series and two AUDA pairs have observations outside the seven-day baseline/target tolerance. All 22 symbols involved are outside the current 173-company universe. No returns or alphas were recalculated. [Post-import preservation audit](2026-09-13-market-data-after-final-import-audit.json); [full pair-level readiness](2026-09-13-market-data-after-final-import-readiness.json).

The same preservation check was repeated on final runtime `1f7f447` at **05:41:00 UTC**, with matching health builds before and after: **710 unique outcomes, zero changed, missing or new rows across 9,940 stored field values** relative to the prior 710-row captures. Independent classification also found zero disagreements. All original 709 rows remain intact, with only previously observed outcome 800 additional. [Final runtime audit](2026-09-13-market-data-after-final-runtime-audit.json); [independent field comparison](2026-09-13-market-data-after-final-runtime-comparison.json).

### Later read-only checkpoints before the next release

At **06:45:03 UTC**, another full read-only audit on `1f7f447` verified all 710 outcomes across 9,940 fields, with zero changes, omissions or new rows relative to the preceding 710-row captures. The **06:48:54 UTC** company census found 173 companies: 20 with a saved memo and 153 without. Only META and LLY gained coverage relative to the original 18-memo census. Every missing ticker and version change is retained. MELI v2 and MO v2 were automatic incremental news patches, not separate regeneration jobs. [Outcome field comparison](2026-09-13-market-data-closeout-comparison.json); [company census comparison](2026-09-13-company-memo-census-closeout-comparison.json); [incremental-patch metadata](2026-09-13-closeout-incremental-patch-history.json).

Work was paused before the pending release was deployed. Read-only snapshots at **20:25:14 and 23:12:30 UTC** still showed the original `1f7f447` deployments and no recorded worker failures since that build became live. These retrospective snapshots do not establish continuous active observation throughout the pause. All 26 industry jobs had succeeded by 08:15:20; all 15 memo regeneration jobs had succeeded, including automatic TSLA filing-event job 15 / v1 at 07:49:38. Neither queue had a queued or running job. The normal **22:39:24 UTC** news pass again failed an ABBV update, so its compatibility repair remained necessary despite the drained queues.

## Owner decisions and next actions

1. **Rotate the four previously exposed provider keys** in their provider consoles and on both Render services: FMP, Alpha Vantage, Polygon and Census. The logging leak is repaired; existing exposed values remain exposed. No key or Render token is rotated by this work.
2. **Approve research-output changes before expanding coverage:** factual claim-to-passage checks, complete-source disclosure, reproducible DCF inputs, observable thesis breakers, and explicit reconciliation of rating versus valuation. The critique supplies exact saved examples and precise implementation proposals. No prompts or rating logic were changed here.
3. **Choose honest evaluation semantics and historical-row handling:** the reported 95–97% thesis rate measures mostly bullish absolute returns. At 90 days, 334 of 338 directional observations were Bullish, with −2.898% median alpha in that bucket and only 31.14% positive alpha. These are overlapping memo evaluations, not independent investment decisions. Decide how to investigate/repair audit candidates and quality-filter evaluation; no historical rows or headline metric were silently changed.
4. **Decide on targeted corpus repair, learning and broader seeding separately.** Historical failed indexing, incomplete sources and hash embeddings can outlive the code fix. Regenerating a known company such as META does not automatically reindex unchanged historical sources. Its comps path can backfill and index newly discovered documents, so actual source-level evidence is needed to establish whether the run invoked chunking. Successful OpenAI embedding queries skip incompatible hash vectors; failed embedding requests can fall back to hash vectors. Saved findings lack the passage/vector provenance needed to certify the retrieved corpus. One META comparison cannot isolate a chunker effect from execution-mode and source differences. Long-term memory remains disabled in production; truthful write handling does not demonstrate a live learning loop. Existing sector patterns saved with an empty `applies_to` list are also not consumed by `patterns_for`; that context behavior remains unchanged.
5. **Reassess worker capacity using the final production window.** The old August peak predates working automatic regeneration. Keep sizing conclusions conditional on observed workload; no plan change is made without that evidence.
6. **Reconcile durable project records.** Notion has two Active MarketMosaic entries, one with the stale AgenticAI path. Local FEAT-001/002/003 statuses conflict with implemented code and Notion completion. The September 12–13 operations and research-quality findings are absent from those task sets. This review leaves Notion and backlog statuses unchanged; the critique contains the exact mappings.

## Preservation and scope

The original branches/worktrees and existing research snapshots are preserved. Operational code changes use additive commits and merges. No broad memo seeding, provider-key rotation, Render-token rotation, paid-plan change, new monitoring loop, prompt rewrite, rating-weight change or headline-KPI redefinition is included. The sole manual validation is job 7 (META v1). Automatic filing-event jobs 3–6 predated the initial census; older user jobs 1–2 are separate historical activity. The later price/fundamental backfill imports data only and does not request memo generation or outcome evaluation.
